# Benchmark

## Measure the full runtime
```bash
$ time <your_command_here>
```

## Measure the percentage of each function

You can use `perf` to measure the percentage of CPU time spent in each function.

- [[perf|Perf Note]]
